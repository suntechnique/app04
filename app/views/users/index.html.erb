<% provide(:title, 'All users') %>

<h1>All users</h1>

<% if signed_in? && current_user.admin? %>
	<section class="user-control-panel">

    <p>
      <div>
	      <%= link_to "add new card", '#', :class => "btn btn-primary" %>
      </div>
    </p>

	</section>
<% end %>

<%= will_paginate %>

<ul class="users-list">
  <%= render @users %>
</ul>

<%= will_paginate %>