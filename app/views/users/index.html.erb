<% provide(:title, 'All users') %>

<h1>All users</h1>

<section class="user-control-panel">
  <!--<h1>управление</h1>-->
  <p>
    <div>
	    <%= link_to "добавить новую карточку", '#', :class => "btn btn-primary" %>
    </div>
  </p>
</section>

<%= will_paginate %>

<ul class="users-list">
  <%= render @users %>
</ul>

<%= will_paginate %>